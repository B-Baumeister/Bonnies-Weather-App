import { getWeather } from "./weather.js";

getWeather();

/* navigator.geolocation.getCurrentPosition(success, error);

function success() {}
function error() {
  alert(`Please allow us to use the location and refresh the page`);
}
 */
